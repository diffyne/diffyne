{
  "name": "diffyne",
  "version": "1.0.0",
  "type": "module",
  "description": "Laravel server-driven UI framework with Virtual DOM",
  "private": true,
  "scripts": {
    "dev": "node build.js --watch",
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.1",
    "terser": "^5.44.0"
  }
}
