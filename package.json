{
  "name": "diffyne",
  "version": "1.0.0",
  "description": "Laravel server-driven UI framework with Virtual DOM",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^7.2",
    "terser": "^5.44.0"
  }
}
